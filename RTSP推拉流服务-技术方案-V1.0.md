# RTSP 推拉流服务 — 技术方案 V1.0

| 项目 | 内容 |
|------|------|
| 项目名称 | RTSP Streaming Service（RTSP 推拉流服务） |
| 版本 | V1.0 |
| 日期 | 2026-08-04 |
| 编制 | Edison（视觉算法助理工程师） |
| 评审人 | Alan（总经理） |
| 状态 | 待评审 |

---

## 1. 项目概述

开发一款集 **RTSP 推流** 与 **RTSP 拉流** 于一体的流媒体服务软件，作为团队视觉系统的视频接入与分发底座。

```
┌─────────────────────────────────────────────────────────────┐
│                  RTSP 推拉流服务（单进程）                     │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌───────────┐               │
│  │ 视频文件  │   │ USB摄像头 │   │ 外部RTSP源 │  ← 输入源     │
│  └──────────┘   └──────────┘   └───────────┘               │
│        └────────────┬──────────────┘                       │
│               ┌─────▼─────┐                                 │
│               │  源抽象层   │  Source 统一接口               │
│               └─────┬─────┘                                 │
│               ┌─────▼─────┐                                 │
│               │  媒体管道   │  透传 / 转码（FFmpeg）          │
│               └─────┬─────┘                                 │
│          ┌──────────┴──────────┐                            │
│   ┌──────▼──────┐        ┌─────▼──────┐                    │
│   │ 内置RTSP服务  │        │ 推流客户端   │                    │
│   │ (Server)     │        │ (Publisher) │                    │
│   └──────┬──────┘        └─────┬──────┘                    │
│          │                     │                            │
│   本地拉流客户端           外部RTSP服务器                     │
└─────────────────────────────────────────────────────────────┘
```

**核心能力：**
1. **RTSP 推流**：将输入源推送到内置 RTSP 服务或任意外部 RTSP 服务器
2. **RTSP 拉流**：从任意 RTSP 地址拉流，支持转发、预览、快照及后续视觉算法处理
3. **内置 RTSP 服务**：软件自带 RTSP Server，对外提供标准 RTSP 拉流地址

**输入源：** 视频文件（支持循环）、USB 摄像头（V4L2）、RTSP 拉流
**输出：** RTSP 推流（内置服务 / 外部服务器）

---

## 2. 需求分析

### 2.1 功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| F1 | RTSP 推流 | 将输入源推送到指定 RTSP 地址（支持鉴权） |
| F2 | RTSP 拉流 | 从外部 RTSP 地址拉取视频流 |
| F3 | 内置 RTSP 服务 | 软件内嵌 RTSP Server，供第三方播放器（VLC/FFplay）直接拉流 |
| F4 | 文件输入 | 支持常见视频格式（MP4/AVI/MKV 等），可循环播放、指定起始位置 |
| F5 | USB 摄像头输入 | Linux V4L2 设备（/dev/video0），可配置分辨率/帧率/像素格式 |
| F6 | RTSP 源输入 | 从外部 RTSP 拉流后作为本服务输入（级联转发） |
| F7 | 多路并发 | 同时管理多路流（推流+拉流混合） |
| F8 | 流管理接口 | REST API：创建/删除流、查询状态；可选 Web 管理界面 |
| F9 | 预览 | 网页/本地预览（MJPEG 或 WebRTC，可选阶段） |
| F10 | 快照 | 任意时刻抓取单帧 JPEG 保存（供算法/告警使用） |

### 2.2 非功能需求

| 类别 | 指标 |
|------|------|
| 延迟 | 透传模式端到端 < 200ms；转码模式 < 500ms |
| 并发 | ≥ 8 路推流；单路流 ≥ 8 个并发拉流客户端 |
| 稳定性 | 7×24 小时运行；断线自动重连 |
| 部署 | 单一二进制 + 可选 FFmpeg 依赖；Linux x86_64 / ARM64 |
| 可维护 | 模块化、配置驱动、日志分级 |
| 扩展性 | 预留视觉算法处理节点（检测/跟踪）插入能力 |

---

## 3. 技术选型

### 3.1 候选方案对比

| 方案 | 语言/框架 | 优点 | 缺点 |
|------|-----------|------|------|
| **A. Go + gortsplib + FFmpeg** ⭐推荐 | Go 1.22 / github.com/bluenviron/gortsplib | RTSP 栈成熟（mediamtx 同源）；单二进制部署；goroutine 天然适合多路并发；FFmpeg 覆盖全部输入源 | FFmpeg 需作为外部依赖（或静态打包） |
| B. GStreamer + gst-rtsp-server | C / Python 绑定 | 管道化灵活，硬件加速生态好 | 依赖重、部署复杂；二次开发门槛高 |
| C. C++ + FFmpeg + Live555 | C++ | 性能极致、完全可控 | 开发量大、RTSP 状态机手写易出 bug |
| D. Python + aiortc/opencv | Python | 原型快、AI 生态好 | 性能弱、并发差，不适合多路生产级 |

### 3.2 推荐方案：Go + gortsplib + FFmpeg

**选型理由：**
1. **gortsplib v3**：Go 社区最成熟的 RTSP/RTP 协议库，mediamtx（原 rtsp-simple-server，GitHub 20k+ star）同源核心，稳定性经过大规模验证
2. **FFmpeg 子进程做媒体接入/转码**：视频文件、V4L2 摄像头、RTSP 拉流全部由 FFmpeg 统一处理，格式覆盖最全；避免 cgo 带来的编译部署复杂度
3. **部署简单**：Go 编译为静态单二进制，运行环境只需 `ffmpeg`（或打包内置）
4. **并发模型**：每路流一个 goroutine 管理，天然支持多路并发；内存占用低
5. **可扩展**：后续接入视觉算法（YOLO 等）可通过"算法处理节点"（进程间管道/共享内存）或 cgo 集成 ONNX Runtime

**技术栈清单：**

| 组件 | 选型 | 用途 |
|------|------|------|
| 语言 | Go 1.22+ | 服务主体、RTSP 协议、并发管理 |
| RTSP 库 | github.com/bluenviron/gortsplib/v3 | 内置 RTSP Server + 拉流/推流 Client |
| 媒体处理 | FFmpeg（子进程模式） | 解码/转码/编码/设备接入（V4L2、文件、RTSP） |
| 硬编解码 | VAAPI / NVENC / QSV（经 FFmpeg，可选） | 降低 CPU 占用 |
| 配置 | YAML（gopkg.in/yaml.v3） | 服务配置、流定义 |
| REST API | 标准库 net/http 或 gin | 流管理接口 |
| 日志 | slog（Go 标准库） | 分级日志 |
| Web 预览 | MJPEG over HTTP（内置） | 轻量预览，零前端依赖 |

---

## 4. 系统架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│ 管理面                                                      │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐              │
│  │ CLI 命令  │  │ REST API  │  │ Web 预览  │              │
│  └──────────┘  └───────────┘  └──────────┘              │
├─────────────────────────────────────────────────────────┤
│ 核心服务层                                                   │
│  ┌───────────────┐  ┌──────────────┐  ┌───────────────┐ │
│  │ 流管理器       │  │ 内置RTSP服务   │  │ 会话/鉴权管理   │ │
│  │ StreamManager│  │ RTSP Server  │  │ Auth/Session  │ │
│  └───────────────┘  └──────────────┘  └───────────────┘ │
├─────────────────────────────────────────────────────────┤
│ 媒体层                                                      │
│  ┌────────────┐  ┌─────────────┐  ┌──────────────────┐  │
│  │ 源抽象      │  │ 媒体管道     │  │ 推/拉流客户端      │  │
│  │ Source     │  │ Pipeline    │  │ RTSP Client      │  │
│  └────────────┘  └─────────────┘  └──────────────────┘  │
├─────────────────────────────────────────────────────────┤
│ 接入层                                                      │
│  ┌────────┐  ┌─────────────┐  ┌───────────┐            │
│  │ FFmpeg  │  │ V4L2 (Linux)│  │ 网络 Sock  │            │
│  │ 进程池  │  │ 摄像头       │  │ TCP/UDP    │            │
│  └────────┘  └─────────────┘  └───────────┘            │
└─────────────────────────────────────────────────────────┘
```

### 4.2 模块职责

| 模块 | 职责 |
|------|------|
| StreamManager | 流生命周期管理（创建/启停/销毁）、路由、状态汇总 |
| 内置 RTSP Server | 监听端口，处理 DESCRIBE/SETUP/PLAY 等信令，向各客户端分发 RTP |
| 源抽象 Source | 统一三种输入源的打开/读取/关闭接口 |
| 媒体管道 Pipeline | 透传模式：源流 → 直接封装 RTP；转码模式：FFmpeg 解码→（算法处理）→编码 |
| 推流客户端 Publisher | 推送到外部 RTSP 服务器，断线重连 |
| 拉流客户端 Consumer | 从外部 RTSP 拉流，供转发/预览/快照/算法 |
| FFmpeg 进程池 | 管理 FFmpeg 子进程生命周期、参数生成、异常重启 |

---

## 5. 核心模块设计

### 5.1 源抽象层（Source）

```go
// 统一源接口
type Source interface {
    Open() error                          // 打开并探测媒体信息（编码/分辨率/帧率）
    MediaInfo() MediaInfo                 // 返回媒体描述（用于 SDP 协商）
    Start(push func(*MediaSample)) error  // 启动读取，通过回调推送媒体样本
    Stop() error
}
```

| 实现 | 底层 | 说明 |
|------|------|------|
| `FileSource` | FFmpeg 解码文件 | 支持循环播放（EOF 后 seek 回 0）、倍速、指定时间偏移 |
| `CameraSource` | FFmpeg + V4L2 | 参数化设备节点、分辨率、帧率、像素格式（YUYV/MJPEG/H264 直出） |
| `RTSPPullSource` | gortsplib Client | 拉取外部 RTSP，透传原始编码流或解码为原始帧 |

**关键设计：** 源在打开时探测编码格式。若源为 H.264/H.265 编码流（如摄像头 H264 直出、外部 RTSP），默认走 **透传模式**（零转码、零 CPU）；仅当需要改变分辨率/帧率/编码格式时才走转码。

### 5.2 媒体管道（Pipeline）

```
透传模式（零开销）:
源编码流(H.264) ──→ RTP打包器 ──→ RTSP Server/Publisher

转码模式:
源 ──→ FFmpeg 解码 ──→ [算法处理节点(预留)] ──→ FFmpeg 编码(H.264) ──→ RTP打包 ──→ 分发
```

转码参数要点：
- 固定 GOP（如 2s 一个关键帧），降低客户端起播延迟
- 编码器优先级：硬件（VAAPI/NVENC/QSV）→ 软件（libx264）
- 码率/分辨率/帧率可配置

### 5.3 内置 RTSP 服务（核心交付）

基于 gortsplib 的 `Server` 实现：

- **监听**：默认 `:8554`（可配置）
- **流路径**：`rtsp://<host>:8554/live/<stream_name>`，每路推流注册一个路径
- **信令处理**：OPTIONS / DESCRIBE（返回 SDP）/ SETUP / PLAY / TEARDOWN / GET_PARAMETER（保活）
- **传输模式**：UDP（默认）、TCP（interleaved）、Multicast（可选），客户端可指定
- **多客户端**：一个推流源可被多个拉流客户端同时消费，各自独立 RTP 会话
- **鉴权**：Basic / Digest，按路径配置用户密码
- **流状态**：无客户端时可按配置暂停源读取（节省资源）或继续推流

### 5.4 推流模块（Publisher）

| 场景 | 目标 | 实现 |
|------|------|------|
| 推流到内置服务 | `rtsp://本机:8554/live/xxx` | 直接注册到本地 StreamManager，零网络开销 |
| 推流到外部服务器 | `rtsp://user:pass@ip:554/path` | gortsplib Publisher 客户端，支持鉴权 |

**可靠性：** 断线自动重连（指数退避 1s→2s→4s…上限 60s + 随机抖动），重连成功后自动续推。

### 5.5 拉流模块（Consumer）

- gortsplib Client 拉取外部流：自动协商编码（H.264/H.265/MJPEG），支持 UDP/TCP
- **断线重连**：同上指数退避策略
- **输出用途**：转推（级联）、Web 预览、快照、算法处理（预留）
- 支持 **拉 A 推 B**（如：拉取摄像头 A 的流，转推给外部平台 B）

### 5.6 管理与监控

**REST API（`/api/v1`）：**

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/streams` | 创建流（指定源类型/参数/输出目标） |
| GET | `/streams` | 列出所有流 |
| GET | `/streams/{id}` | 查询单路流状态（帧率/码率/客户端数） |
| DELETE | `/streams/{id}` | 删除流 |
| POST | `/streams/{id}/snapshot` | 抓取快照 |
| GET | `/health` | 健康检查 |

**监控指标：** 实时帧率、码率、RTP 丢包率、客户端连接数、FFmpeg 进程状态、内存/CPU。

---

## 6. 关键流程

### 6.1 推流流程（文件源 → 内置 RTSP 服务）

```
1. 创建流：POST /streams { source: file, path: /tmp/a.mp4, loop: true, target: local:/live/cam1 }
2. FileSource.Open() → FFmpeg 探测：H.264 1280x720 25fps
3. 判定透传模式（源已是 H.264）
4. 注册路径 /live/cam1 到内置 RTSP Server
5. FFmpeg 解码输出 H.264 Annex-B 流 → 回调 → RTP 打包（NAL 切分，大帧用 FU-A 分片）
6. 播放器请求：VLC 打开 rtsp://host:8554/live/cam1
   → OPTIONS → DESCRIBE(返回SDP) → SETUP(协商UDP/TCP) → PLAY
7. 服务端按 RTP 时钟(90kHz) 持续发送；GET_PARAMETER 保活
8. 删除流：TEARDOWN 清理，FFmpeg 进程回收
```

### 6.2 拉流流程（外部 RTSP → 预览/转发）

```
1. 创建流：POST /streams { source: rtsp, url: rtsp://192.168.1.64:554/Streaming/Channels/101, target: local:/live/ch1 }
2. RTSPPullSource 连接 → DESCRIBE → 解析 SDP → SETUP → PLAY
3. 收到 RTP → 解包还原 H.264 流 → 透传给本地路径 /live/ch1
4. 断线 → 指数退避重连 → 自动恢复
```

---

## 7. 关键技术点与难点

| # | 难点 | 对策 |
|---|------|------|
| 1 | RTP 打包正确性（H.264 NAL 切分、FU-A 分片、SPS/PPS 注入） | 使用 gortsplib 封装层，重点做单测 + 与 VLC/FFplay 互测 |
| 2 | RTP 时间戳同步（90kHz 时钟基准） | 以源 PTS 为基准换算，统一时钟 |
| 3 | 客户端起播延迟 | 转码固定 GOP；服务端缓存最近关键帧，新客户端从关键帧起播 |
| 4 | USB 摄像头兼容性（格式/分辨率协商失败） | 启动时探测 V4L2 能力列表，自动降级（如 1080p→720p，H264→MJPEG→YUYV） |
| 5 | FFmpeg 子进程管理 | 封装进程池：参数模板、超时检测、异常自动重启、输出日志解析（错误码/帧统计） |
| 6 | 文件循环播放无缝 | EOF 检测 → 重新 seek → 关键帧对齐后续推（避免时间戳跳变） |
| 7 | TCP/UDP 模式自适应 | RTSP 信令协商 + RTP over RTSP(TCP interleaved) 回退，保证 NAT/防火墙环境可用 |
| 8 | 多客户端带宽控制 | 每客户端独立发送队列，慢客户端丢弃旧帧（不阻塞推流源） |
| 9 | 硬编解码降级 | 启动时探测 VAAPI/NVENC 可用性，失败自动回落 libx264 软编 |

---

## 8. 性能与可靠性设计

- **零拷贝优先**：RTP 打包直接引用解码输出缓冲，减少内存拷贝
- **内存池**：帧缓冲复用，避免 GC 压力
- **断线重连**：指数退避 + 抖动，防重连风暴
- **看门狗**：每路流监控帧间隔，超时（如 5s 无帧）判定异常 → 重启源
- **优雅退出**：SIGTERM → TEARDOWN 所有会话 → 回收 FFmpeg 进程
- **资源上限**：每路流码率上限、总并发限制，防资源耗尽
- **日志分级**：debug/info/warn/error，按流 ID 过滤，便于定位

---

## 9. 测试验证方案

| 层级 | 内容 | 工具/方法 |
|------|------|-----------|
| 单元测试 | SDP 生成/解析、RTP 打包、重连状态机、鉴权 | Go test |
| 集成测试 | 本机起服务 → FFplay/VLC 拉流验证音画正常 | FFplay、VLC、mediamtx 互测 |
| 源覆盖测试 | 视频文件（MP4/AVI）、USB 摄像头（真实设备）、外部 RTSP（海康/大华模拟源） | 真实设备 + 合成源（FFmpeg testsrc 生成标准测试视频） |
| 并发压测 | 8 路推流 + 每路 8 客户端拉流同时运行 | 自定义压测客户端（基于 gortsplib） |
| 稳定性测试 | 24h 连续运行，监控内存/CPU/帧率波动 | 监控脚本 + 日志 |
| 故障注入 | 断网重连、FFmpeg 进程 kill、摄像头拔插 | 脚本模拟 |

**验收标准：**
- VLC/FFplay 可流畅播放内置服务所有流，起播 < 1s
- 透传模式 CPU 占用 < 5%（单路 1080p）
- 24h 无崩溃、无内存泄漏趋势
- 断网 60s 内自动恢复推流

---

## 10. 开发计划（建议 4~5 周，1 人）

| 阶段 | 周期 | 里程碑 | 交付物 |
|------|------|--------|--------|
| P1 骨架 | 第 1 周 | 项目结构、配置、日志、流管理器、文件源透传链路打通 | 可运行骨架 + FFplay 可拉流 |
| P2 内置 RTSP 服务 | 第 2 周 | 多路径、多客户端、UDP/TCP、鉴权 | 完整 RTSP Server 模块 |
| P3 全输入源 + 推流 | 第 3 周 | USB 摄像头源、RTSP 拉流源、外部推流 Publisher、重连机制 | 三大输入源全通 |
| P4 管理面 | 第 4 周 | REST API、快照、Web 预览、监控指标 | 可远程管理 |
| P5 测试收尾 | 第 5 周 | 单元/集成/压测/24h 稳定性、文档 | 测试报告 + 使用文档 |

**P1 完成后即可进行第一轮演示**（文件源推流 → VLC 拉流）。

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 第三方摄像头 RTSP 兼容性问题 | 拉流失败 | 优先适配主流品牌（海康/大华）标准 RTSP；提供兼容性测试清单 |
| FFmpeg 子进程通信开销 | 转码链路延迟略高 | 透传模式兜底；转码场景接受 <500ms 指标 |
| 硬件编码器驱动环境差异 | 转码 CPU 高 | 自动探测+软编回退；文档说明驱动安装要求 |
| 交付时间紧 | 进度风险 | 模块化开发，P1/P2 为核心，P4 Web 预览可裁剪 |

---

## 12. 附录：参考项目

- **mediamtx**（github.com/bluenviron/mediamtx）：Go 实现的生产级 RTSP/RTMP/WebRTC 媒体服务器，本项目 RTSP 技术同源
- **go2rtc**（github.com/AlexxIT/go2rtc）：Go 实现，FFmpeg 子进程做转码的成熟实践，本项目媒体接入方式参考
- **GStreamer rtsp-server**：备选方案的参考实现
- **Live555**：经典 RTSP 库，协议细节参考（RFC 2326 / 7826、RFC 3550 RTP）

---

*本方案待 Alan 评审确认后进入开发阶段。*

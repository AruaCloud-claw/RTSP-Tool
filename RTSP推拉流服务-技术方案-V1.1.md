# RTSP 推拉流服务 — 技术方案 V1.2（桌面版）

| 项目 | 内容 |
|------|------|
| 项目名称 | RTSP Streaming Service（RTSP 推拉流服务） |
| 版本 | V1.2（已评审确认，进入开发） |
| 日期 | 2026-08-04 |
| 编制 | Edison（视觉算法助理工程师） |
| 评审人 | Alan（总经理）✅ 已确认 |
| 状态 | **开发中** |

**V1.2 变更说明（Alan 评审确认项）：**
1. ✅ 架构原则确认：**Electron 仅承载 UI，媒体核心引擎保持 Go 实现**
2. ✅ 平台范围确认：**仅 Windows 10/11 x64**，不做 Linux 版
3. ✅ 预览方案确认：**首版即采用 WebRTC 低延迟预览**（<300ms），MJPEG 仅作降级备选
4. 工期基准：6~7 周（含 WebRTC 预览 +1 周）

---

## 1. 项目概述

开发一款集 **RTSP 推流** 与 **RTSP 拉流** 于一体、带 **可视化桌面界面** 的流媒体服务软件，作为团队视觉系统的视频接入与分发底座，可在 Windows 上一键安装部署。

```
┌─────────────────────────────────────────────────────────────┐
│  Electron 桌面应用（Windows）                                 │
│  ┌────────────────────┐  ┌───────────────────────────────┐  │
│  │ 渲染进程（UI）       │←→│ 主进程（Node.js）              │  │
│  │ Vue3 + Element Plus │  │ 引擎进程管理 / API 代理 / 日志 │  │
│  └────────────────────┘  └──────────────┬────────────────┘  │
│                                         │ REST API + WebSocket│
│  ┌──────────────────────────────────────▼─────────────────┐  │
│  │  Go 核心媒体引擎（rtsp-engine.exe）                     │  │
│  │  ┌──────────┐ ┌──────────┐ ┌───────────┐              │  │
│  │  │ 源抽象层  │→│ 媒体管道  │→│ 内置RTSP服务│              │  │
│  │  │文件/摄像头│ │透传/转码  │ │ + 推流/拉流 │              │  │
│  │  │/RTSP源   │ │          │ │  客户端    │              │  │
│  │  └──────────┘ └──────────┘ └───────────┘              │  │
│  └──────────────────────────────────────┬─────────────────┘  │
│                                         │ 子进程               │
│  ┌──────────────────────────────────────▼─────────────────┐  │
│  │  FFmpeg（ffmpeg.exe）解码 / 编码 / 设备接入进程池        │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**核心能力：**
1. **RTSP 推流**：将输入源推送到内置 RTSP 服务或任意外部 RTSP 服务器
2. **RTSP 拉流**：从任意 RTSP 地址拉流，支持转发、预览、快照及后续视觉算法处理
3. **内置 RTSP 服务**：软件自带 RTSP Server，对外提供标准 RTSP 拉流地址（VLC/FFplay 可直接播放）
4. **可视化 UI**：流管理、实时预览、状态监控、配置管理全部图形化操作

**输入源：** 视频文件（支持循环）、USB 摄像头（Windows DirectShow / Linux V4L2）、RTSP 拉流
**输出：** RTSP 推流（内置服务 / 外部服务器）+ UI 内实时预览

---

## 2. 需求分析

### 2.1 功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| F1 | RTSP 推流 | 将输入源推送到指定 RTSP 地址（支持鉴权） |
| F2 | RTSP 拉流 | 从外部 RTSP 地址拉取视频流 |
| F3 | 内置 RTSP 服务 | 软件内嵌 RTSP Server，供第三方播放器（VLC/FFplay）直接拉流 |
| F4 | 文件输入 | 支持常见视频格式（MP4/AVI/MKV 等），可循环播放 |
| F5 | USB 摄像头输入 | Windows DirectShow / Linux V4L2，可配置分辨率/帧率 |
| F6 | RTSP 源输入 | 从外部 RTSP 拉流后作为本服务输入（级联转发） |
| F7 | 多路并发 | 同时管理多路流（推流+拉流混合） |
| F8 | 可视化 UI | 图形化管理所有流、实时预览、状态监控 |
| F9 | 实时预览 | UI 内直接观看每路流画面 |
| F10 | 快照 | 任意时刻抓取单帧 JPEG 保存 |
| F11 | 系统服务化 | 可注册为 Windows 后台服务/开机自启（可选） |

### 2.2 非功能需求

| 类别 | 指标 |
|------|------|
| 平台 | **Windows 10/11 x64（唯一目标）** |
| 部署 | 一键安装包（NSIS），内置 Go 引擎 + FFmpeg，无需手动配环境 |
| 延迟 | 透传模式端到端 < 200ms；转码模式 < 500ms；**UI 预览（WebRTC）< 300ms** |
| 并发 | ≥ 8 路推流；单路流 ≥ 8 个并发拉流客户端 |
| 稳定性 | 7×24 小时运行；断线自动重连；UI 崩溃不影响引擎 |
| 可维护 | 模块化、配置驱动、日志分级、UI 内查看日志 |

---

## 3. 技术选型

### 3.1 候选方案对比（媒体引擎）

| 方案 | 语言/框架 | 优点 | 缺点 |
|------|-----------|------|------|
| **A. Go + gortsplib + FFmpeg** ⭐推荐 | Go 1.22 / github.com/bluenviron/gortsplib | RTSP 栈成熟（mediamtx 同源）；跨平台（Windows/Linux）；goroutine 适合多路并发；FFmpeg 覆盖全部输入源 | FFmpeg 需打包分发 |
| B. GStreamer + gst-rtsp-server | C/Python | 管道化灵活 | Windows 下 GStreamer 部署极繁琐，不推荐 |
| C. C++ + FFmpeg + Live555 | C++ | 性能极致 | 开发量大、Windows 编译链复杂 |
| D. 全 JS（Node RTSP 库） | Node.js | 与 Electron 同语言 | RTSP 库生态不成熟、稳定性差、并发弱，**不推荐作为引擎** |

### 3.2 桌面 UI 框架对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **Electron** ⭐推荐 | 生态最成熟（VS Code/Slack 同款）；前端技术栈丰富；打包分发方案成熟 | 体积大（~80-120MB）、内存占用偏高 |
| Tauri | 体积小（~10MB）、性能好 | 需 Rust 技术栈；Windows WebView2 依赖 |
| Wails | Go 原生、体积小 | 生态较小，复杂 UI 组件少 |
| PyQt/PySide | Python 生态 | 界面美观度和前端生态弱，打包大 |

**推荐 Electron**：团队将大量使用前端组件（表格、表单、图表、视频播放），Electron 生态最省力；体积问题在现代 PC 上可接受。

### 3.3 总体架构原则（关键决策）

> **Electron 只做 UI 壳，媒体引擎用 Go 独立进程，两者通过 REST API + WebSocket 通信。**

理由：
1. gortsplib 是生产级验证过的 RTSP 实现，Node 生态无同等替代
2. 引擎独立进程 → UI 卡死/崩溃不影响推流服务（7×24 稳定性保障）
3. Go 引擎可独立测试、独立复用（未来可单独部署为 Linux 服务）
4. 前后端通过 HTTP/WS 解耦，接口清晰，UI 可单独迭代

**技术栈清单：**

| 层 | 组件 | 用途 |
|----|------|------|
| UI | Electron + Vue3 + Element Plus + ECharts | 桌面界面、组件库、监控图表 |
| 引擎 | Go 1.22 + gortsplib/v3 | RTSP Server、源管理、推/拉流客户端 |
| 媒体 | FFmpeg（Windows 官方构建，随包分发） | 解码/转码/编码/设备接入 |
| 通信 | REST API（/api/v1）+ WebSocket | 控制指令 + 实时状态推送 |
| 预览 | **WebRTC（pion/webrtc，低延迟 <300ms）**；MJPEG 仅作降级备选 | UI 内实时画面 |
| 打包 | electron-builder（NSIS） | Windows 安装包，含引擎+FFmpeg |
| 硬编解码 | NVENC / QSV（经 FFmpeg，可选） | 降低 CPU 占用 |

---

## 4. 系统架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│ 桌面层（Electron，Windows）                                    │
│  ┌───────────────┐   ┌──────────────────────────────────┐   │
│  │ 渲染进程 UI     │←─→│ 主进程（Node）                    │   │
│  │ · 流管理页面    │   │ · 引擎生命周期管理（拉起/守护/退出）│   │
│  │ · 实时预览      │   │ · REST/WS 代理                   │   │
│  │ · 监控图表      │   │ · 日志采集                       │   │
│  │ · 系统设置      │   │ · 自动更新（可选）                │   │
│  └───────────────┘   └──────────────────┬───────────────┘   │
├─────────────────────────────────────────┼───────────────────┤
│ 引擎层（Go 单进程）                       │ REST + WS         │
│  ┌──────────────┐ ┌──────────────┐ ┌─────▼────────────┐    │
│  │ 流管理器      │ │ 内置RTSP服务  │ │ HTTP API 服务     │    │
│  │ StreamManager│ │ RTSP Server  │ │ (8554 + 8080)    │    │
│  └──────┬───────┘ └──────┬───────┘ └──────────────────┘    │
│  ┌──────▼─────────────────▼───────┐                        │
│  │ 源抽象层（文件/摄像头/RTSP）      │                        │
│  │ 媒体管道（透传/转码/预览抽帧）     │                        │
│  │ 推流客户端 / 拉流客户端           │                        │
│  └──────┬──────────────────────────┘                        │
│  ┌──────▼────────────┐                                     │
│  │ FFmpeg 子进程池     │  dshow(摄像头) / 文件 / RTSP 源      │
│  └───────────────────┘                                     │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 模块职责

| 模块 | 职责 |
|------|------|
| Electron 渲染进程 | 全部 UI：流列表、创建流向导、预览窗、监控面板、设置页 |
| Electron 主进程 | 引擎进程管理（启动/守护/退出）、API 代理、日志转发、开机自启 |
| Go 引擎（StreamManager） | 流生命周期、路由、状态汇总、REST API 服务 |
| 内置 RTSP Server | 监听 :8554，信令处理、多客户端 RTP 分发、鉴权 |
| 源抽象 Source | 统一文件/摄像头/RTSP 三种输入的打开/读取/关闭 |
| 媒体管道 Pipeline | 透传 / 转码 / 预览抽帧（MJPEG） |
| 推/拉流客户端 | 外部 RTSP 推流、拉流、断线重连 |
| FFmpeg 进程池 | 子进程管理、参数生成、异常重启 |

---

## 5. 核心模块设计

### 5.1 源抽象层（Source）

```go
type Source interface {
    Open() error                          // 打开并探测媒体信息（编码/分辨率/帧率）
    MediaInfo() MediaInfo                 // 返回媒体描述（用于 SDP 协商）
    Start(push func(*MediaSample)) error  // 启动读取，通过回调推送媒体样本
    Stop() error
}
```

| 实现 | 底层 | 平台适配 |
|------|------|----------|
| `FileSource` | FFmpeg 解码文件 | 跨平台，支持循环播放 |
| `CameraSource` | FFmpeg 输入 | **Windows：DirectShow**（`-f dshow -i video="摄像头名"`）；Linux：V4L2 |
| `RTSPPullSource` | gortsplib Client | 跨平台，透传原始编码流 |

**关键设计：** 源打开时探测编码格式。H.264/H.265 编码流默认走 **透传模式**（零转码、零 CPU）；仅需改分辨率/帧率/编码时才转码。

### 5.2 媒体管道（Pipeline）

```
透传模式（零开销）:
源编码流(H.264) ──→ RTP打包 ──→ RTSP Server / 外部推流

转码模式:
源 ──→ FFmpeg 解码 ──→ [算法处理节点(预留)] ──→ FFmpeg 编码 ──→ RTP打包 ──→ 分发

预览抽帧（独立于推流链路）:
任一路流 ──→ 抽帧（1~10fps）──→ MJPEG ──→ HTTP 预览接口（UI 播放）
```

- 转码固定 GOP（2s），降低客户端起播延迟
- 编码器优先级：NVENC/QSV 硬件 → libx264 软编
- 预览抽帧与推流解耦，预览卡顿不影响正式推流

### 5.3 内置 RTSP 服务

基于 gortsplib `Server`：
- 监听 `:8554`（可配置）
- 流路径：`rtsp://<host>:8554/live/<stream_name>`
- 信令：OPTIONS / DESCRIBE / SETUP / PLAY / TEARDOWN / GET_PARAMETER
- 传输：UDP / TCP(interleaved) / Multicast（可选）
- 多客户端独立会话；Basic/Digest 鉴权
- 无客户端时可选暂停源读取（省资源）

### 5.4 推流 / 拉流模块

| 模块 | 功能 | 可靠性 |
|------|------|--------|
| 推流 Publisher | 推到内置服务（本地直连）或外部服务器（rtsp://user:pass@ip/path） | 断线重连：指数退避 1s→60s + 抖动 |
| 拉流 Consumer | 从外部 RTSP 拉流，支持转推/预览/快照/算法 | 同上重连策略；支持拉 A 推 B 级联 |

### 5.5 可视化 UI 设计

**页面结构：**

```
┌──────────────────────────────────────────────────────┐
│  RTSP Streaming Service          [流管理] [监控] [设置] │
├──────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐              │
│  │ 流卡片1  │  │ 流卡片2  │  │ 流卡片3  │  ← 流管理页   │
│  │ [预览]   │  │ [预览]   │  │ [预览]   │   + 新建流    │
│  │ 状态:推流中│  │ 状态:拉流中│  │ 状态:异常 │   向导      │
│  └─────────┘  └─────────┘  └─────────┘              │
│  ┌──────────────────────────────────────────────┐   │
│  │ 预览大窗格（点击卡片放大，多路网格 1/4/9 宫格）  │   │
│  └──────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────┤
│  监控页：帧率/码率/客户端数实时曲线（ECharts）          │
│  设置页：服务端口、鉴权、转码参数、开机自启、日志查看     │
└──────────────────────────────────────────────────────┘
```

**核心交互：**

| 功能 | 交互 |
|------|------|
| 新建流 | 向导式：选源类型（文件/摄像头/RTSP）→ 配置参数 → 选输出（内置/外部）→ 一键启动 |
| 摄像头选择 | 下拉自动枚举 Windows 已接入摄像头（FFmpeg dshow 探测） |
| 实时预览 | 点击流卡片 → 大窗格播放（**WebRTC**，<300ms）；支持多路网格同屏 |
| 流控制 | 启停、暂停、删除、快照、复制 RTSP 地址（一键复制给 VLC） |
| 状态监控 | 卡片角标实时状态；监控页曲线图；异常告警提示 |
| 日志 | UI 内查看引擎/FFmpeg 日志，按级别过滤 |

**关键问题：浏览器不能直接播放 RTSP** → 解决方案：

| 方案 | 延迟 | 实现 | 定位 |
|------|------|------|------|
| **WebRTC（pion/webrtc）** | <300ms | 引擎侧 pion/webrtc 转 RTP；H.264 SPS/PPS 注入；localhost 直连无需 STUN | **首版主方案** |
| MJPEG over HTTP | 300-800ms | 引擎抽帧转 JPEG | 降级备选（WebRTC 不可用时） |

**WebRTC 预览设计（关键实现路径）：**
1. 引擎内置 WebRTC 服务（基于 **pion/webrtc**，mediamtx/go2rtc 同款技术）
2. 信令走引擎 HTTP API：UI 创建 `RTCPeerConnection` → 生成 Offer → 引擎 Answer → `onicecandidate` 交换（本机直连，仅 host candidate）
3. H.264 视频 track：源编码流直接 RTP 打包（透传）或转码后打包，SPS/PPS 周期性注入
4. UI 渲染进程：`video.srcObject = stream`，原生播放零延迟

### 5.6 管理与监控

**REST API（`/api/v1`，引擎 8080 端口）：**

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/streams` | 创建流（源类型/参数/输出目标） |
| GET | `/streams` | 列出所有流及状态 |
| GET | `/streams/{id}` | 查询单路流详情 |
| DELETE | `/streams/{id}` | 删除流 |
| POST | `/streams/{id}/start` `/stop` | 启停流 |
| POST | `/streams/{id}/snapshot` | 快照 |
| GET | `/streams/{id}/preview.mjpg` | 预览流（MJPEG） |
| GET | `/cameras` | 枚举 Windows 摄像头 |
| GET | `/health` | 健康检查 |
| WS | `/ws` | 实时状态推送（帧率/码率/告警） |

**监控指标：** 帧率、码率、RTP 丢包、客户端数、FFmpeg 进程状态、内存/CPU。

---

## 6. 关键流程

### 6.1 推流流程（文件源 → 内置 RTSP 服务）

```
1. UI 新建流：文件源 + 循环播放 + 输出=内置 /live/cam1
2. 引擎：FileSource.Open() → FFmpeg 探测（H.264 1080p25）→ 判定透传
3. 注册路径 /live/cam1 到内置 RTSP Server
4. FFmpeg 输出 H.264 → RTP 打包（NAL 切分 / FU-A 分片）→ 按 90kHz 时钟发送
5. VLC 打开 rtsp://host:8554/live/cam1 → 起播 < 1s
6. UI 同步显示：状态=推流中，帧率/码率实时刷新
```

### 6.2 拉流流程（外部 RTSP → 预览 + 转推）

```
1. UI 新建流：RTSP 源 + 目标=内置 /live/ch1
2. 引擎：连接 → DESCRIBE → SDP 解析 → SETUP → PLAY
3. 收 RTP → 解包还原 H.264 → 透传给 /live/ch1 + 抽帧供 UI 预览
4. 断线 → 指数退避重连 → 自动恢复，UI 状态自动更新
```

---

## 7. 关键技术点与难点

| # | 难点 | 对策 |
|---|------|------|
| 1 | RTP 打包正确性（NAL 切分、FU-A、SPS/PPS 注入） | gortsplib 封装 + 与 VLC/FFplay 互测 |
| 2 | 浏览器无法直接播 RTSP | MJPEG 预览（MVP）→ WebRTC（进阶） |
| 3 | Windows 摄像头枚举与兼容 | FFmpeg dshow 探测设备列表；分辨率/格式失败自动降级 |
| 4 | FFmpeg 子进程管理（Windows 无 SIGTERM 语义） | 用进程组 + stdin 指令/优雅 kill；超时强杀；异常自动重启 |
| 5 | 文件循环无缝 | EOF → seek 0 → 关键帧对齐，时间戳归一化 |
| 6 | 引擎崩溃守护 | Electron 主进程监听引擎退出码 → 自动拉起（可配置） |
| 7 | TCP/UDP 自适应 | RTSP 协商 + TCP interleaved 回退，防火墙环境可用 |
| 8 | 多客户端带宽 | 每客户端独立队列，慢客户端丢旧帧不阻塞源 |
| 9 | 硬编解码降级 | 启动探测 NVENC/QSV，失败自动回落软编 |
| 10 | 打包体积与安装 | electron-builder NSIS；FFmpeg 裁剪（仅需的组件，约 30-60MB） |

---

## 8. 性能与可靠性设计

- **引擎独立进程**：UI 崩溃不影响推流服务（核心保障）
- **预览与推流解耦**：预览抽帧独立链路，预览压力不影响正式流
- **断线重连**：指数退避 + 抖动
- **看门狗**：每流监控帧间隔，5s 无帧判定异常 → 自动重启源
- **优雅退出**：UI 退出 → 通知引擎 TEARDOWN 所有会话 → 回收 FFmpeg
- **资源上限**：每流码率上限、总并发限制
- **日志分级**：debug/info/warn/error，UI 可查

---

## 9. 测试验证方案

| 层级 | 内容 | 工具/方法 |
|------|------|-----------|
| 单元测试 | SDP、RTP 打包、重连状态机、鉴权 | Go test |
| 集成测试 | 本机起引擎 → FFplay/VLC 拉流；UI 全流程操作 | FFplay、VLC、mediamtx 互测 |
| 源覆盖 | 视频文件、真实 USB 摄像头（Windows dshow）、外部 RTSP 源 | 真机 + FFmpeg testsrc 合成源 |
| 并发压测 | 8 路推流 + 每路 8 客户端 | 自定义压测客户端（gortsplib） |
| 稳定性 | 24h 连续运行，监控内存/CPU/帧率 | 监控脚本 + 日志 |
| 故障注入 | 断网、FFmpeg 进程 kill、摄像头拔插、UI 强杀 | 脚本模拟 |
| 安装验证 | 全新 Windows 机器一键安装 → 开箱即用 | 虚拟机/实体机 |

**验收标准：**
- Windows 全新机器安装包一键部署，无需手动装环境
- UI 完整操作流：建流 → 预览 → 快照 → 停止 → 删除，全程图形化
- VLC 可播放所有内置流，起播 < 1s
- 透传模式单路 1080p CPU < 5%（引擎）
- 24h 无崩溃、无内存泄漏；UI 强杀后引擎继续推流

---

## 10. 开发计划（建议 6~7 周，1 人）

| 阶段 | 周期 | 里程碑 | 交付物 |
|------|------|--------|--------|
| P0 工程搭建 | 第 1 周 | Electron 壳 + Go 引擎进程管理框架 + REST 通信打通 | 双进程可启动，UI 显示引擎状态 |
| P1 核心链路 | 第 2 周 | 文件源 + 内置 RTSP 服务 + 透传 | FFplay 可拉流；UI 建流+复制地址 |
| P2 UI 主体 + WebRTC | 第 3 周 | 流管理页 + **WebRTC 低延迟预览** + 状态卡片 | 完整 UI 操作闭环（文件源，预览 <300ms） |
| P3 全输入源 | 第 4 周 | Windows 摄像头源（dshow）+ RTSP 拉流源 + 外部推流 + 重连 | 三大输入源全通 + 级联 |
| P4 增强功能 | 第 5 周 | 快照、监控图表、转码模式、鉴权、日志页、设置页 | 功能齐备 |
| P5 打包与测试 | 第 6~7 周 | electron-builder 打包、压测、24h 稳定性、安装验证、文档 | 安装包 + 测试报告 + 使用文档 |

**P2 结束（第 3 周末）即可进行完整演示**：文件推流 + UI 预览 + VLC 拉流。

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| Electron 体积大（100MB+） | 分发体积 | 可接受（桌面工具常态）；FFmpeg 裁剪优化 |
| 第三方摄像头 RTSP 兼容性 | 拉流失败 | 优先适配主流品牌标准 RTSP；兼容性测试清单 |
| Windows 摄像头驱动/格式差异 | 采集失败 | dshow 探测 + 自动降级（分辨率/像素格式） |
| 引擎-UI 双进程调试复杂度 | 开发效率 | 接口先行（REST 契约），引擎可独立命令行调试 |
| 硬编码器环境差异 | 转码 CPU 高 | 自动探测 + 软编回退；文档说明驱动要求 |
| WebRTC 信令/ICE 复杂度 | 预览链路 bug | 本机直连仅 host candidate，无需 STUN/TURN；pion 库成熟可参照 mediamtx |
| 工期 6~7 周偏紧 | 进度风险 | 模块化；开机自启、多路同屏等列为可裁剪项 |

---

## 12. 附录：参考项目

- **mediamtx**（github.com/bluenviron/mediamtx）：Go 生产级媒体服务器，RTSP 技术同源，也提供 WebRTC 参考
- **go2rtc**（github.com/AlexxIT/go2rtc）：Go + FFmpeg 子进程转码 + WebRTC 的成熟实践，本项目预览方案参考
- **Electron**：桌面壳（VS Code/Slack 同款技术）
- **FFmpeg Windows 构建**：gyan.dev / BtbN 官方构建，打包分发

---

*本方案待 Alan 评审确认后进入开发阶段。*
